package org.pubky.pulse.android.compose

import androidx.compose.runtime.Composable

/**
 * Phase 1 scaffold placeholder so the Compose module compiles and the Compose
 * compiler plugin is wired. Replaced by `PulseFeedbackView` (Phase 7) and
 * `PulseQuestionnaireView` + `pulseScreen` (Phases 8–9).
 */
@Composable
internal fun PulseComposePlaceholder() {
}
